---
title: Body
category: study
---

# Body

主体段的质量取决于论证方式是否多样，而不是句子是否华丽。五种功能各挑一种，一段一个。与 [[Opening]]、[[Closing]] 同属 [[Exam Essay]] 的三段式骨架。

## 现成句

| 句子 | 用在哪 |
|---|---|
| A case in point is `__`. | 举例，最万能 |
| Take `__` as an example: it illustrates that `__`. | 举例，带解释 |
| Statistics reveal that `__` percent of `__`. | 数据支撑（数字可虚构，但要有零有整） |
| The primary reason lies in the fact that `__`. | 分析原因 |
| Consequently, `__` gives rise to `__`. | 推演结果 |
| In contrast, `__` tends to `__`. | 正反对比 |
| Admittedly, `__` does have its merits. | 让步，先承认对方 |
| Nevertheless, the drawbacks cannot be overlooked. | 让步后转折回己方 |
| What deserves more attention is that `__`. | 递进，推出更强论点 |
| To put it another way, `__`. | 换说法，救场用 |

> [!warning] 让步不是示弱
> `Admittedly` 之后必须接转折。只写让步不写回击，等于帮对方论证。

## 段落配方

```chain
主题句 | 一句说清这段要证明什么 | 必写
展开 | 举例或说理 | 一至两句
回扣 | 把例子拉回主题句 | 别漏
```

> [!danger] 最常见的失分点
> 举完例子不回扣。例子本身不证明任何事，只有把例子拉回主题句，论证才闭合。

## 自检

- 每段是否只有一个中心，且写在了第一句
- 举例之后有没有一句话说明这个例子证明了什么
